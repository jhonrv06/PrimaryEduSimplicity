---
// src/components/SliderScript.astro
import { eventSliderMoov } from '../scripts/doomInicio.ts';
---

<script type="module" is:inline>
    // IMPORTA AQUÍ la función usando la ruta que Vite expone en dev y en build:
    import { eventSliderMoov } from '/src/scripts/doomInicio.ts';
    
    window.addEventListener('DOMContentLoaded', () => {
        eventSliderMoov();
    });
  </script>