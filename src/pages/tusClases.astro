---
import Tus_Clases from '../components/Tus_Clases.astro';
import MainLayout from '../layouts/MainLayout.astro';
import {getMedia, getRecurses, getPeriodos, getGrados} from '../lib/WorpressH';

const pagename = "Transición";

const habilidades = await getRecurses("grado");

const grados = await getGrados();
console.log(habilidades)
const periodos = await getPeriodos();

const gradoId = grados.find( grado =>{ return grado.name === pagename });

const media = await getMedia(gradoId.id);

//console.log(periodos)

---

<MainLayout title="Tus cursos">
	<Tus_Clases media={media} periodos={periodos} />
</MainLayout>